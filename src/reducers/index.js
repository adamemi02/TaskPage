import { combineReducers } from "redux";
import graph from "./graph";

export default combineReducers({
  graph
});

// aici am zis ca folosim combine reducers pentru a adauga toti reducerii de care avem nevoie